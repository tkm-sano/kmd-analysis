# 研究概要・ロードマップ v17

文書ID: `DOC-RESEARCH-OVERVIEW-V17`
役割: `PRIMARY_ENTRY`
ライフサイクル: `CURRENT`
作成日: `2026-09-03`
最終更新日: `2026-09-03`
現行正本: `reproducibility/config/traffic_simulation/current_network_completion_authority_v17.yml`

状態: `ADOPTED / CURRENT` の研究ナビゲーション文書
基準日: 2026-09-03
対象範囲: 研究計画、依存関係、マイルストーン、ゲート、現在の状態のみ

本書は研究全体の人間向け入口である。機械可読な[現行道路網完成正本](reproducibility/config/traffic_simulation/current_network_completion_authority_v17.yml)、受入成果物、設定、スキーマ、manifestを置き換えない。値または受入状態が矛盾する場合は、現行正本とその参照成果物を優先する。

## 研究の問い

> 量子技術の発展が、移動技術を通じて都市社会・都市経済にどのような変化をもたらしうるか。

本研究では、技術変化と社会・経済変化をつなぐ媒介としてモビリティ／物流を扱う。量子技術から都市社会への直接的な因果効果を証明するものではない。最終分析は**将来シナリオ分析**であり、仮定、モデル化した機構、シミュレーション結果、解釈を区別しなければならない。

## 状態語彙

| ラベル | 意味 |
|---|---|
| `ADOPTED / CURRENT` | 現在採択されている正本、定義、または研究上の立場 |
| `DONE` | 受入条件を満たし、下流利用が許可された状態 |
| `NEXT` | 直近の完了マイルストーンに続く当面の実行段階 |
| `PLANNED` | 作業とゲートは定義済みだが未完了の段階 |
| `CANDIDATE` | 後続レビューの候補であり、未採択の選択肢 |
| `UNRESOLVED` | 必要な判断または値が未確定の状態 |
| `FUTURE` | 前提条件がまだ満たされていない将来作業 |

コード、文書、探索的結果が存在するだけでは、その段階を`DONE`とはしない。

## 概念モデル

```text
量子技術
  → モビリティ／物流能力
  → 計画／政策
  → 事業／経済活動
  → 都市社会／都市経済
```

矢印は明示したシナリオの下で検討する機構を示し、確立済みの直接因果を示すものではない。実証・実装経路では、量子能力が最適化能力を変え、最適化能力が実行可能な物流計画を変え、実行された配送結果が計画・事業上の選択肢を拡大または制約しうる。各接続には固有の仮定、証拠、感度分析が必要である。

## 現在地

| 項目 | 現在値 |
|---|---|
| 現在の完了マイルストーン | **M1 道路網準備完了 — `DONE`** |
| 現在の研究段階 | **経路基準 — `NEXT`** |
| 直近の作業 | **どの配送instanceを対象に、どのStop間routing costを生成するかを正式に定義する** |

現在受け入れられている道路網状態（`ADOPTED / CURRENT`）:

| 項目 | 受入値 |
|---|---|
| 需要基準 | `DONE` |
| Requests / Stops生成 | `DONE` |
| 三層Formal道路網完成 | `DONE` |
| SUMO道路網構築 | `DONE` |
| 車線・速度・通行権限検証 | `PASS` |
| Stop mapping | `39,956 / 39,956` |
| Formal道路網受入 | `ACCEPTED` |
| 受入フラグ | `FORMAL_NETWORK_ACCEPTED = true` |
| 道路網 | `three_tier.net.xml` |
| SHA-256 | `4625dbbc150cbcf72964bed0e90a8b33fe03f190ff4264aecaaf89e3aab0e40f` |

受入道路網のpathと受入証拠は現行正本から解決する。本書が記録するのはナビゲーション層の状態だけであり、第二の受入正本にはならない。

## 分析パイプラインと依存関係

```text
外部データ／オープンデータ
  → 基準需要
  → 将来需要パラメータ化
  → Requests / Stops
  → Structural道路網
  → Formal道路網完成
  → SUMO具現化
  → Stop mapping
  → Formal道路網受入
  → 経路基準
  → 共通配送instance
  → 古典最適化
  → QUBO定式化
  → QAOA／量子最適化
  → 配送シミュレーション
  → 充足評価
  → 計画／事業解釈
  → 将来社会解釈
```

これは端から端までの分析経路であり、すべての成果物を単一の直線的jobで生成するという意味ではない。

- 現行Requests / Stopsは基準需要から生成された受入済み基準成果物であり、将来需要パラメータ化の完了を意味しない。
- 将来需要パラメータ化は`PLANNED`のシナリオ変換であり、同じ統制interfaceを通じてシナリオ別Requests / Stopsを生成する。
- 道路網構築と需要準備は上流の別分岐であり、Stop mappingと経路基準で合流する。
- 感度・頑健性分析は、経路、道路網fallback属性、需要、電池、最適化、QUBO、シナリオを横断する。
- 再現性・公開凍結は最終的な横断ゲートである。

## 研究段階、ゲート、下流依存関係

### 段階1 — 経路基準（`NEXT`）

**目的:** 受入済みFormal道路網上で配送最適化に必要な移動費用を固定する。

| ゲート項目 | 定義 |
|---|---|
| 必須入力 | 受入済みSUMO道路網、受入済みStop mapping、Requests / Stops、depot、配送車種 |
| 作業 | 経路対象範囲、Depot → Stop、Stop → Stop、Stop → Depotの費用、旅行時間、距離、到達可能性、経路provenanceを定義 |
| 出力 | version付き経路方法、対象OD費用成果物、検証report、provenance／command |
| 受入ゲート | 経路方法を固定し、全組合せと決めつけずに必要なOD費用を生成し、経路検証が`PASS`し、成果物を再現可能にする |
| 下流依存 | Stage 2 共通配送instance |

`39,956 × 39,956`の全組合せ経路計算は、採択済み前提ではない。最初の作業は、評価対象の配送instanceと、そのinstanceが必要とするStop組を正確に定義することである。

未解決事項は、depot位置、instanceの抽出・選択規則、車種詳細、経路費用の対象範囲、経路生成方法、到達不能組の扱い、provenance契約である。

### 段階2 — 共通配送instance（`PLANNED`）

**目的:** 需要、経路、車両制約を、古典手法と量子手法で共有するsolver非依存問題へ統合する。

| ゲート項目 | 定義 |
|---|---|
| 必須入力 | 受入済みRequests / Stops、選択済みdepot、Stage 1経路費用、旅行時間・距離、車両制約、電池制約 |
| 必須内容 | Requests、Stops、荷物相当量、depot、経路費用、旅行時間、距離、車両制約、電池制約、provenance |
| 出力 | version付きschema、validator、本番生成config／command、再現可能な本番instance |
| 受入ゲート | schemaを固定し、validatorが利用可能かつ合格し、placeholderのない本番instanceを再現可能に生成して受け入れる |
| 下流依存 | Stage 3 古典最適化、Stage 4 QUBO定式化 |

repository確認では、`05_src/optimization/common_delivery_instance.py`は**現行checkoutに存在しない**。過去のGit objectには、requests、stops、depot、vehicles、node順、距離・旅行時間・車両energy行列、provenance hashを要求するsolver非依存validator候補がある。この履歴設計はレビュー材料になるが、`ADOPTED / CURRENT`ではなく、現行validatorとして利用できず、本Stageのゲートを満たさない。復元、改訂、置換のいずれを採るかは`UNRESOLVED`である。

### 段階3 — 古典最適化（`PLANNED`）

**目的:** 量子手法との比較基準となる古典基準を確立する。

定式化の`ADOPTED / CURRENT`な研究目的は、電池その他の実行可能性要件をhard制約として扱い、**計画上充足する荷物相当量を最大化すること**である。厳密な数理定式化とoptimizerは、Stage 3ゲートまで`PLANNED / UNRESOLVED`である。

| ゲート項目 | 定義 |
|---|---|
| 必須入力 | 受入済み共通配送instance、固定した目的意味論、固定した実行可能性checker、solver予算、seed |
| 必須定式化 | 決定変数、目的関数、経路実行可能性、車両容量、電池実行可能性、充足需要、追加の採択済み制約 |
| 出力 | 固定した数理定式化、実装、正当性fixture、本番基準、provenance |
| 受入ゲート | 定式化を固定し、solverを実装し、小規模instanceの正当性を検証し、本番基準を生成する |
| 下流依存 | Stage 4比較、Stage 6配送シミュレーション |

optimizer algorithm、fleet数、配送容量、電池値、計算予算は`UNRESOLVED`である。妥当そうな候補があるだけでは採択しない。

### 段階4 — 量子最適化（`PLANNED`）

#### 段階4A — QUBO定式化

固定した古典問題をQUBOへ変換する。目的の同値性、制約penalty、変数mapping、係数scaling、正当性を検査する。入力は受入済み共通配送instanceと固定済み古典定式化、出力はversion付き定式化とencoder／decoder契約である。同値性と実行可能解の意味論を示すまでゲートは閉じる。

#### 段階4B — QUBO検証

小規模instanceで古典最適解とQUBO最適解を比較する。出力には厳密なfixture、penalty設定、decode済み解、実行可能性検査、不一致診断を含める。低いQUBO energyだけでなく、宣言した同値条件で一致することを受入条件とする。

#### 段階4C — QAOA

検証済みQUBOをQAOAまたは明示的に採択した別の量子最適化経路へ接続する。受入済みQUBO、実行backend仮定、回路parameter、seed、sampling／decoding／repair規則、測定境界を入力として要求する。QAOA depthと量子hardware仮定は`UNRESOLVED`である。

#### 段階4D — 古典手法と量子手法の比較

比較候補は、解品質、充足荷物相当量、実行可能性、実行時間、問題規模、回路・resource要件である。共通instanceと共通評価規則を必須とする。量子優位性は仮定せず、優位性がない結果やscale不能も有効な結果とする。

| ゲート項目 | 定義 |
|---|---|
| 必須入力 | 受入済みStage 2 instance、受入済みStage 3基準、検証済みQUBO、固定済み量子実行・比較protocol |
| 出力 | QUBO定式化・検証証拠、量子候補解、公平な古典対量子比較 |
| 受入ゲート | 再現可能な設定の下で4A～4Dが合格し、共通checkerで実行可能性を評価する |
| 下流依存 | Stage 6配送シミュレーション、Stage 9機構解釈 |

QUBO係数、penalty値、QAOA depth、optimizer選択、backend／hardware仮定は`UNRESOLVED`である。

### 段階5 — 将来技術・需要シナリオ（`PLANNED`）

技術シナリオと社会・需要シナリオは、一つの実験で組み合わせる場合でも別入力とする。

| シナリオ群 | 候補次元 | 現在の状態 |
|---|---|---|
| 技術シナリオ | EV電池能力、車両能力、経路・最適化能力、量子計算能力 | `CANDIDATE`。parameter値と証拠は未固定 |
| 社会・需要シナリオ | 人口変化、配送需要総量変化、空間需要変化 | `CANDIDATE`。**将来需要パラメータ化は未完了** |

| ゲート項目 | 定義 |
|---|---|
| 必須入力 | 受入済み基準、証拠に基づくシナリオ定義、parameter provenance、採択する場合の対象範囲・比較年 |
| 出力 | 分離したversion付き技術・需要scenario configと、事前登録した組合せ |
| 受入ゲート | 基準を上書きせず、parameter、出典、範囲、変換規則、基準比較を固定・検証する |
| 下流依存 | scenario別Requests / Stops、共通配送instance、最適化、simulation、解釈 |

需要成長率とシナリオ年は`UNRESOLVED`である。

### 段階6 — 配送シミュレーション（`PLANNED`）

**目的:** SUMOまたは明示的に受け入れたsimulation環境で最適化計画を実行する。

| ゲート項目 | 定義 |
|---|---|
| 必須入力 | 受入済み道路網、受入済み共通配送instance、古典・量子計画、交通／scenario config、実行seed |
| 出力 | 実経路trace、旅行時間、待機、電池使用、完了配送、失敗配送、混雑相互作用 |
| 受入ゲート | 計画を再現可能に変換し、実行・失敗accountingを検証し、planとrunのprovenanceを保持する |
| 下流依存 | Stage 7充足評価 |

最適化出力は計画解、simulation出力はモデル上実現した挙動であり、両者を同一視しない。

### 段階7 — 充足評価（`PLANNED`）

主要指標:

```text
delivery_fulfillment_rate
  = delivered_parcel_equivalent / total_parcel_equivalent
```

正式利用前に、分母の対象範囲、時間範囲、到達不能・除外需要の扱いを固定する。

| 指標の役割 | 指標 |
|---|---|
| 主要 | `delivery_fulfillment_rate` |
| 補助 | 配送済み荷物相当量、未充足荷物相当量、車両利用率、旅行時間、距離、電池使用量、到達不能需要 |

| ゲート項目 | 定義 |
|---|---|
| 必須入力 | 受入済みsimulation結果、固定した評価母集団・分母、共通指標実装 |
| 出力 | 不確実性・失敗分解を伴う主要・補助指標 |
| 受入ゲート | 式、単位、対象範囲、分母、除外、集約を固定し、fixtureでevaluatorを検証し、結果を再現する |
| 下流依存 | Stage 8計画・事業解釈、Stage 10頑健性分析 |

### 段階8 — 計画・事業解釈（`PLANNED`）

**目的:** 技術結果を条件付きの社会・経済的意味へ接続する。

| 解釈分岐 | 候補となる問い |
|---|---|
| 計画・政策 | 物流accessibility、service coverage、infrastructure要件、政策選択、resilience |
| 事業・経済 | 実行可能な配送規模、運用制約、service area、事業機会、resource要件 |

| ゲート項目 | 定義 |
|---|---|
| 必須入力 | Stage 7証拠、宣言済みscenario、不確実性・感度結果、外部文脈証拠 |
| 出力 | 限界を明示した条件付きの計画・政策・事業・経済解釈 |
| 受入ゲート | 各主張をモデル結果とscenario仮定へ追跡し、代替説明と不確実性を示し、simulationだけから直接的社会因果を主張しない |
| 下流依存 | Stage 9将来社会解釈 |

### 段階9 — 将来社会解釈（`PLANNED`）

最終的に検討する機構:

```text
量子能力
  → 最適化能力
  → モビリティ／物流能力
  → 配送充足
  → 計画／事業上の選択肢
  → 都市社会／都市経済
```

| ゲート項目 | 定義 |
|---|---|
| 必須入力 | Stage 4・7・8の証拠、scenario証拠、明示した機構仮定と限界 |
| 出力 | 統合した将来社会解釈と範囲を限定した結論 |
| 受入ゲート | 各接続で測定・モデル結果とscenario仮定を区別し、競合説明と限界を扱い、量子優位を前提としない |
| 下流依存 | Stage 10頑健性分析と最終結論 |

### 段階10 — 感度・頑健性分析（`PLANNED`）

最低限の感度領域は、経路仮定、推論・fallback道路網属性、需要仮定、電池仮定、最適化parameter、QUBO penalty、scenario仮定である。

| ゲート項目 | 定義 |
|---|---|
| 必須入力 | 受入済み基準結果、事前登録した不確実parameter・範囲、再実行・比較protocol |
| 出力 | 感度matrix、頑健性要約、失敗境界、解釈変化 |
| 受入ゲート | 重要仮定を体系的に変化させ、結論を頑健、条件付き、根拠不足に分類する |
| 下流依存 | Stage 11凍結と公開主張 |

### 段階11 — 再現性・公開凍結（`FUTURE`）

| ゲート項目 | 定義 |
|---|---|
| 必須入力 | 主張対象となる全Stageの受入済み出力・検証証拠 |
| 出力 | 凍結した現行正本pointer、config、schema、受入成果物、hash、software version、command、Portal、本研究概要 |
| 受入ゲート | 再現性auditが問題なく、全公開主張が凍結証拠へ追跡でき、Portalと概要が一致し、内部参照が有効である |
| 下流依存 | 公開／提出／archive release |

## マイルストーン

| マイルストーン | 状態 | 目的 | 入力 | 必須出力 | 受入条件 | 次の依存マイルストーン |
|---|---|---|---|---|---|---|
| M1 道路網準備完了 | `DONE` | 受入済みで配送利用可能なFormal／SUMO道路網を提供 | Structural道路網、三層完成policy、SUMO具現化、検証、Stops | 受入済み`three_tier.net.xml`、受入・mapping証拠、hash、provenance | `FORMAL_NETWORK_ACCEPTED = true`、車線・速度・通行権限検証`PASS`、Stops mapping `39,956 / 39,956` | M2 経路準備完了 |
| M2 経路準備完了 | `NEXT` | 配送instanceが必要とする対象限定の移動費用を固定 | M1道路網、mapping済みStops、Requests、depot、車種、経路範囲 | 経路方法、必要OD費用、検証、provenance | 方法固定、必要OD集合完備、検証`PASS`、成果物再現可能 | M3 最適化準備完了 |
| M3 最適化準備完了 | `FUTURE` | 共通問題を凍結し古典基準を確立 | M2費用、需要、depot・車両・電池制約 | 受入済み共通配送instance、validator、古典定式化・solver・正当性・本番基準 | Stage 2・3ゲート合格 | M4 量子比較準備完了 |
| M4 量子比較準備完了 | `FUTURE` | 優位性を前提とせずQUBO／QAOA経路を検証・比較 | M3 instance／基準、QUBO／QAOA protocol | 検証済みQUBO、量子結果、公平な比較証拠 | 小規模同値性と共通evaluator比較に合格 | M5 scenario simulation準備完了 |
| M5 scenario simulation準備完了 | `FUTURE` | 基準と受入済み将来scenarioを実行 | M3／M4計画、受入済み技術・需要scenario、SUMO config | 再現可能なsimulation run、計画から実行までのtrace | scenario入力受入、run・失敗検証合格 | M6 評価完了 |
| M6 評価完了 | `FUTURE` | 充足度と補助結果を定量化 | M5出力、固定指標契約 | 不確実性を伴う充足率・補助指標 | 指標定義・分母固定、evaluator検証、結果再現 | M7 研究解釈完了 |
| M7 研究解釈完了 | `FUTURE` | 計画・事業・将来社会への含意を解釈 | M4比較、M6評価、感度証拠、外部文脈 | 機構に基づき範囲を限定した解釈と限界 | 主張が証拠・scenarioへ追跡でき、根拠のない因果主張を避ける | M8 再現性・公開凍結 |
| M8 再現性・公開凍結 | `FUTURE` | 公開用の証拠連鎖全体を凍結 | 全受入済みmilestone・成果物 | 正本・config・schema・成果物hash、version、command、Portal、概要 | 再現・link audit合格、全公開主張が凍結証拠へ追跡可能 | 公開／archive |

## 研究全体の状態

| 領域 | 状態 | 主な成果物 | 次の作業 |
|---|---|---|---|
| 基準需要 | `DONE` | 統制済み基準需要成果物・config | 基準入力として保持し、将来需要と混同しない |
| 将来需要 | `PLANNED` | 受入済みparameterizationなし | 証拠、scenario年・率、空間変換、検証を固定 |
| Requests / Stops | `DONE` | 受入済みRequests / Stops、39,956 Stops | 全Stopsを各instanceに使うと仮定せず対象範囲を選択 |
| Formal道路網 | `DONE` | 三層完成Formal道路網 | 現行正本を通じてのみ使用 |
| SUMO道路網 | `DONE` | `three_tier.net.xml` | Stage 1道路網入力として使用 |
| Stop mapping | `DONE` | mapping受入 39,956 / 39,956 | 対象経路端点を定義 |
| 道路網受入 | `DONE` | `network_acceptance.json`、`FORMAL_NETWORK_ACCEPTED = true` | 正本とhash bindingを保持 |
| 経路 | `NEXT` | 受入済み成果物なし | 配送instanceの経路対象範囲・方法を定義 |
| 共通配送instance | `PLANNED` | 履歴上の候補契約のみ。現行checkoutには不在 | 経路範囲固定後に契約の扱いを決定 |
| 古典最適化 | `PLANNED` | 比較protocolのみ。受入済みsolver・結果なし | 共通配送instanceゲート後に定式化を固定 |
| QUBO | `PLANNED` | 受入済み定式化なし | 古典同値性の対象固定後にのみ定義 |
| QAOA | `FUTURE` | 受入済み実装・結果なし | 検証済みQUBOにのみ接続 |
| simulation | `PLANNED` | 本pipelineの受入済み配送実行なし | 計画・scenario受入後に実行 |
| 充足評価 | `PLANNED` | 本roadmap内の候補式 | 正式結果前に分母・範囲・evaluatorを固定 |
| 解釈 | `FUTURE` | 概念機構 | 受入済み結果を条件付きscenarioとして解釈 |
| 再現性 | `PLANNED` | 現行正本・repository indexあり。最終凍結は未完了 | milestoneごとに成果物・version・command凍結を拡張 |

## 未解決判断台帳

次の項目を推測または暗黙に補完してはならない。

| 判断 | 状態 | 最初に必要となるゲート |
|---|---|---|
| 配送instanceの選択・抽出とStop範囲 | `UNRESOLVED` | Stage 1 |
| depot位置 | `UNRESOLVED` | Stage 1 |
| 配送車種詳細 | `UNRESOLVED` | Stage 1 |
| 経路方法と必要OD集合 | `UNRESOLVED` | Stage 1 |
| 共通配送instance契約の扱い | `UNRESOLVED` | Stage 2 |
| fleet数 | `UNRESOLVED` | Stage 2 |
| 車両配送容量 | `UNRESOLVED` | Stage 2 |
| 電池値・modelとenergy cost意味論 | `UNRESOLVED` | Stage 2 |
| 古典optimizer algorithmと計算予算 | `UNRESOLVED` | Stage 3 |
| QUBO係数・penalty値 | `UNRESOLVED` | Stage 4A／4B |
| QAOA depth、optimizer、shots、backend、hardware仮定 | `UNRESOLVED` | Stage 4C |
| scenario年 | `UNRESOLVED` | Stage 5 |
| 需要成長率・空間変化parameter | `UNRESOLVED` | Stage 5 |
| 充足指標分母の範囲・時間範囲 | `UNRESOLVED` | Stage 7 |

## リポジトリ・Portal統合

`20260903_20260903_RESEARCH_OVERVIEW.md`は日付入り正本である。`RESEARCH_OVERVIEW.md`はGitHub、Portal、CLI、外部link互換性のために残す、内容を持たないstable pointerである。これにより内容の二重管理を避けつつ、正本filenameで作成日・更新日を示す。現行Stage 1～11 roadmapは本書以外に存在しない。`RESEARCH_STATUS.md`は、より狭い旧21段階source taxonomyを使う過去の生成dashboardである。日付入りproject management文書は履歴上の設計・状態記録であり、並列の現行roadmapではない。

- 現行機械可読道路網正本: [current_network_completion_authority_v17.yml](reproducibility/config/traffic_simulation/current_network_completion_authority_v17.yml)
- Repository入口index: [research_repository_index_v17.yml](reproducibility/indexes/research_repository_index_v17.yml)
- 人間向けnavigation map: [日付入りresearch repository map v17](reproducibility/indexes/20260903_20260903_research_repository_map_v17.md)
- Repository inventory説明: [research_repository_inventory_v17.md](reproducibility/indexes/research_repository_inventory_v17.md)、機械可読inventory: [`markdown_inventory_v17.yml`](reproducibility/indexes/markdown_inventory_v17.yml)
- Portal stage graph: [research_map_v1.yml](reproducibility/config/research_portal/research_map_v1.yml)
- 統合研究CLI: [日付入りresearch CLI reference](docs/20260903_20260903_research_cli.md)（`./research commands`が正本command index）
- 現行pipeline運用manual: [Research Pipeline Reference](RESEARCH_PIPELINE_REFERENCE.md)（各stageのinput/output、command、authority、validation、acceptance、handoffを参照）
- 古典・量子比較の公平性note: [optimization_comparison_protocol.md](05_src/traffic_simulation/optimization_comparison_protocol.md)

Repository Indexは本書を主要な人間向け概要として指す。Portalは同じStage 1～11のlabelと順序を使用し、別のstage taxonomyを定義しない。Portalと本書はnavigation・解釈層であり、受入truthは機械可読正本と受入成果物に置く。

## 検証チェックリスト

- [x] 受入道路網状態が現行正本と一致する。
- [x] `FORMAL_NETWORK_ACCEPTED = true`である。
- [x] 道路網milestoneは`M1 Network Ready — DONE`である。
- [x] `Routing Baseline`は`NEXT`である。
- [x] 将来Stageを`DONE`としていない。
- [x] 受入道路網名とSHA-256が現行正本と一致する。
- [x] Stop mappingは`39,956 / 39,956`である。
- [x] depot、fleet数、電池値、需要成長率、QUBO係数、QAOA depth、optimizer、scenario年、量子hardware仮定、配送容量を創作していない。
- [x] 本書は人間向けnavigation文書であり、機械可読正本を置き換えない。

---

**現在のマイルストーン = M1 道路網準備完了 — `DONE`**
**現在の研究段階 = 経路基準**
**直近の作業 = 配送instanceの経路対象範囲を定義する**
