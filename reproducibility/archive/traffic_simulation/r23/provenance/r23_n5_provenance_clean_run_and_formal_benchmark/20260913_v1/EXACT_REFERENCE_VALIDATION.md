# Exact reference validation

The clean runner independently enumerated all 120 directed customer permutations from the routing CSV before optimization. Each selected instance had one optimum and matched the recorded decoded route and objective within 1e-12. The solver's stored exact route was not used as the independent reference.

