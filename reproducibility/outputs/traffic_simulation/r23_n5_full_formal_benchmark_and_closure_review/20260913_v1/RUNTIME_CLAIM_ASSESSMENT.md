# Runtime claim assessment

For one fixed n=5 rank01 objective evaluation, baseline/V4 total wall-time ratio is `51.2982x`; Aer ratio is `1.6765x`; postprocessing ratio is `8758.9x`. The result is a valid single-workload measurement with output equivalence and fresh-process boundaries. It is `N5_FORMAL_BENCHMARK_PASSED_WITH_LIMITATIONS`: one repetition and elevated host load limit population-level claims. Historical 33.7x is `FORMALLY_NOT_REPRODUCED` and `NOT_COMPARABLE_TO_NEW_BENCHMARK` because its definition is absent.

