# Memory claim assessment

Using the identical process `ru_maxrss` metric, baseline peak RSS was 211,405,768 KiB and V4 was 675,432 KiB, a single-run ratio of `312.9934x`. This is `N5_FORMAL_BENCHMARK_PASSED_WITH_LIMITATIONS`; repetition and host-load limitations remain. Historical 327x is `FORMALLY_NOT_REPRODUCED` and remains `PRELIMINARY_ONLY`; the new ratio is approximately 313x and must not be relabeled 327x.

