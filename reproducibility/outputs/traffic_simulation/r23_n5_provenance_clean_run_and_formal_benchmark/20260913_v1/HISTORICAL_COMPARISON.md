# Historical comparison

Exact routes and objectives are numerically equivalent for all three instances. Clean-run probabilities are consistent with optimizer variation, not exact byte matches. Rank01 retains cap termination and `success=false`; rank02/03 remain successful but have different nfev and runtime. Classification: route/reference `NUMERICALLY_EQUIVALENT`; probability, nfev and runtime `CONSISTENT_WITH_EXPECTED_OPTIMIZER_VARIATION`; no scientific mismatch.

