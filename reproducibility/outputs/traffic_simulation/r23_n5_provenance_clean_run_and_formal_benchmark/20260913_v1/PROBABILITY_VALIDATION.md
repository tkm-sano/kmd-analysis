# Probability validation

For each clean run, `probability_total` was finite and within 1e-12 of one, and `probability_total - P_feasible - invalid_probability_mass` was zero within machine precision. P_optimal remained below P_feasible and renormalization was false. Full statevector tables are intentionally not serialized; clean-run source, final parameters, trace and output hashes are retained.

