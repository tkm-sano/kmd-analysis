# Gate A sign-off

## Verdict

**`GATE_A_ACCEPTED_WITH_LIMITATIONS`**

Acceptance criteria:

1. Source horizon — frozen: `R24-SOURCE-DAY-2026-01-01-v1`.
2. Benchmark customer semantics — frozen: one positive-demand stable building equals one model customer.
3. Routing proxy semantics — frozen and explicitly non-physical.
4. Eligibility rule — frozen, including current-baseline compatibility and directed depot reachability.
5. Exception rule — frozen with detect/count/disposition/reason contract.
6. Demand conservation — frozen and verified through saved building aggregation.
7. Instance Generation interface — frozen for the three future generator families.

Observed entrance, actual service-event identity, dispatch, wave and shift evidence are absent and are not Gate A blockers for this controlled benchmark. Claims that they are observed remain prohibited.

## Limitations

- The source demand is synthetic; original generator provenance is incomplete, so regeneration is not claimed.
- No observed physical stop, entrance, curb/loading point, carrier service event or carrier operation exists in the evidence.
- Historical all-population R03 mapping is run_2. Current accepted run_3 Routing Baseline endpoints/reachability cover only an old 10-customer fixture.
- A 39,956-customer candidate frame is supported, but final `C_eligible` is not materialized until vehicle/access compatibility and `DEP_006` directed reachability are evaluated.
- No operational representativeness, representative small-n, dispatch, fleet, capacity, mass or one-tour claim is accepted.

## Deprecated current-design assumptions

The following are retained only as historical artifacts and must not be inherited as current authority: building = observed customer; building = physical delivery stop; mandatory service-event reconstruction; mandatory dispatch batch; daily demand = one vehicle-tour load; automatic primary `Q=2000 kg`; representative small-n claim; quartile × tertile sampling; nested PPS.

No random, controlled or fixed-anchor instance was generated by this sign-off.

`NEXT_EXECUTABLE_TASK = vehicle-class evidence review`

