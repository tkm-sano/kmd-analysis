# Bit ordering

An independent permutation enumeration over the pinned routing inputs produced 120 routes, 120 unique row-major encodings, and successful route→bits→route round-trips for rank01, rank02 and rank03. `qaoa.py` and `metrics.py` reverse labels at the Qiskit boundary consistently. Result: `VERIFIED` for encoding/decoding.
