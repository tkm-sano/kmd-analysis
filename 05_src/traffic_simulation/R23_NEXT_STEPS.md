# R23 next steps（現行）

`R23_LIMITED_SCALING_METHODOLOGY_REVIEW`とresource remediationは完了し、n=5 exact CPU Aer resource preflight PASSおよび実行authorization V2を発行した。次の科学タスクは`R23_N5_SCALING_EXECUTION`だが、科学実行はこのtaskでは行っていない。n>=6は現行exact CPU Aer statevectorでは実行非推奨である。

その後は Capacity → Time Window → Battery/SOC → Charging → Multiple Vehicles → Full EVRP integration → Full EVRP validation の順。Full-EVRP R20=BLOCKED、R21=NOT_STARTED。
